%

rgb   = out.rgb.signals.values(:,:,:,end);      % H x W x 3 (uint8)
depth = out.depth.signals.values(:,:,end);      % H x W (double/single)
Tcam  = out.camera_transform.signals.values(:,:,end); % 4x4